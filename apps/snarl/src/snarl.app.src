%% -*- erlang -*-
{application, snarl,
 [{description, "AAA Server"},
  {vsn, "0.7.0p5"},
  {registered, []},
  {applications,
    [kernel,
     stdlib,
     riak_core,
     fifo_db,
     fifo_dt,
     fifo_utils,
     fifo_spec,
     ranch,
     folsom_ddb,
     mdns_server_lib,
     lager,
     fifo_lager,
     libsnarlmatch,
     jsx,
     snmp,
     crypto,
     bcrypt,
     bitcask,
     snarl_oauth,
     merklet,
     esqlite,
     yubico,
     uuid
     ]},
  {mod, {snarl_app, []}},
  {env,
   [{r, 2},
    {n, 3},
    {w, 3},
    {snarl_user, {3, 2, 3}},
    {snarl_role, {3, 2, 3}},
    {auto_gc, disabled},
    {user_sync_timeout,  {1, week}},
    {role_sync_timeout, {1, week}},
    {user_bucket_size, 50},
    {role_bucket_size, 50}
   ]}
 ]}.
